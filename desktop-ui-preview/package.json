{
  "name": "agents-doctor-desktop-preview",
  "version": "0.6.0",
  "private": true,
  "description": "Local desktop prototype for running AGENTS.md Doctor without a terminal.",
  "type": "module",
  "main": "main.mjs",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "icon": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/generate-icon.ps1",
    "parent:install": "node scripts/ensure-parent-install.mjs",
    "prelauncher": "npm run icon",
    "launcher": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/create-windows-shortcut.ps1",
    "predev": "npm run icon && npm run parent:install && npm --prefix .. run build",
    "dev": "electron .",
    "prepackage:win": "npm run parent:install && npm --prefix .. run build",
    "package:win": "node scripts/package-windows.mjs",
    "presmoke": "npm run icon && npm run parent:install && npm --prefix .. run build",
    "safety:scan": "node scripts/runtime-safety-scan.mjs",
    "smoke": "npm run safety:scan && node scripts/smoke.mjs && npm run launcher"
  },
  "devDependencies": {
    "@electron/packager": "^20.0.0",
    "electron": "^42.3.0"
  }
}
